[project]
name = "module-code-review"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "superbase-sdk",
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "anthropic>=0.40",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
